components:
  schemas:
    ScoreDto:
      properties:
        score:
          type: integer
        userId:
          type: string
          format: uuid
        quizId:
          type: string
          format: uuid
