components:
  schemas:
    UserDto:
      properties:
        id:
          type: string
          format: uuid
        login:
          type: string
        role:
          type: string
        createdAt:
          type: string
          format: date
