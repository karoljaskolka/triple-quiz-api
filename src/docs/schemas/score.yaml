components:
  schemas:
    Score:
      properties:
        id:
          type: string
          format: uuid
        score:
          type: integer
        userId:
          type: string
          format: uuid
        quizId:
          type: string
          format: uuid
        createdAt:
          type: string
          format: date
