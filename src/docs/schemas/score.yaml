components:
  schemas:
    Score:
      properties:
        id:
          type: string
          format: uuid
        score:
          type: integer
        user:
          $ref: '#/components/schemas/User'
        quizId:
          type: string
          format: uuid
        createdAt:
          type: string
          format: date
